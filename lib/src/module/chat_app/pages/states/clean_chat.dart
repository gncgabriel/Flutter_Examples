import 'package:flutter/material.dart';

class CleanChat extends StatelessWidget {
  const CleanChat({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Select a chat to talk"),
      ),
    );
  }
}
